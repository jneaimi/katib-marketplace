[project]
name = "katib-marketplace"
version = "0.1.0"
description = "Curation pipeline for the Katib marketplace"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.34",       # S3-compatible client for Cloudflare R2
    "pyyaml>=6.0",       # parse pack.yaml
    "requests>=2.31",    # POST to the registry API
]

[tool.uv]
dev-dependencies = []
